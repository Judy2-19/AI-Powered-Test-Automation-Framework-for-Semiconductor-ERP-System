# 测试策略

## 优先级
- P0：登录、核心列表可见、核心提交成功（必须测）
- P1：编辑/删除/权限差异
- P2：异常态、边界值

## AI 必须遵守
- 优先生成可回放的 Playwright Test 脚本
- 定位优先：getByRole / getByLabel / getByTestId
- 禁止 `waitForTimeout` 硬等（除非注释说明原因）
- 每个页面先交 P0，再谈覆盖率
- 破坏性操作必须用可回收测试数据（e2e_ 前缀）

## 不测
- 第三方稳定外链
- 纯视觉像素级还原
- 需真实人审的审批流终态