# 页面：销售主单管理

- 路由：`/orders/order0`
- 前置：已登录（通过 storageState 复用登录态）
- 页面标题：米格实验室 - 销售主单管理
- 侧边栏路径：订单管理 → 销售主单管理

## 关键区域与精确定位器

| 区域 | 定位器 | 说明 |
|------|--------|------|
| 面包屑 | `getByRole('link', { name: '销售主单管理' })` | 当前页面面包屑 |
| 主单ID输入框 | `getByRole('textbox', { name: '主单ID' })` | 文本输入 |
| 订单状态下拉框 | `getByRole('combobox', { name: '订单状态' })` | 下拉选择 |
| 审批状态下拉框 | `getByRole('combobox', { name: '审批状态' })` | 下拉选择 |
| 技术交付下拉框 | `getByRole('combobox', { name: '技术交付' })` | 下拉选择 |
| 客户姓名下拉框 | `getByRole('combobox', { name: '客户姓名' })` | 下拉选择 |
| 客户机构下拉框 | `getByRole('combobox', { name: '客户机构' })` | 下拉选择 |
| 搜索按钮 | `getByRole('button', { name: '搜索' })` | 搜索操作 |
| 重置按钮 | `getByRole('button', { name: '重置' })` | 重置表单 |
| 新增按钮 | `getByRole('button', { name: '新增' })` | 新增主单 |
| 报价单按钮 | `getByRole('button', { name: '报价单' })` | 报价单操作 |
| 合同按钮 | `getByRole('button', { name: '合同' })` | 合同操作 |
| 费用确认按钮 | `getByRole('button', { name: '费用确认' })` | 费用确认操作 |
| 对账按钮 | `getByRole('button', { name: '对账' })` | 对账操作 |
| 开票按钮 | `getByRole('button', { name: '开票' })` | 开票操作 |
| 付款按钮 | `getByRole('button', { name: '付款' })` | 付款操作 |
| 导出按钮 | `getByRole('button', { name: '导出' })` | 导出数据 |
| 展开按钮 | `getByRole('button', { name: '展开' })` | 显示更多搜索条件 |
| 我的订单标签 | `getByRole('tab', { name: '我的订单' })` | 顶部标签页 |
| 协同订单标签 | `getByRole('tab', { name: '协同订单' })` | 顶部标签页 |
| 全部订单标签 | `getByRole('tab', { name: '全部订单' })` | 顶部标签页 |
| 销售子单标签 | `getByRole('tab', { name: '销售子单' })` | 底部详情标签页 |
| 采购订单标签 | `getByRole('tab', { name: '采购订单' })` | 底部详情标签页 |
| 实验信息标签 | `getByRole('tab', { name: '实验信息' })` | 底部详情标签页 |
| 销售合同标签 | `getByRole('tab', { name: '销售合同' })` | 底部详情标签页 |
| 样品流转标签 | `getByRole('tab', { name: '样品流转' })` | 底部详情标签页 |
| 添加按钮 | `getByRole('button', { name: '添加' })` | 底部详情区域添加按钮 |
| 关联按钮 | `getByRole('button', { name: '关联' })` | 销售合同关联按钮 |
| 数据表格 | `page.locator('.el-table').first()` | Element Plus 表格组件 |

## P0 场景

1. 页面加载完成，表格数据可见（验证页面可访问性）
2. 搜索功能：输入主单ID搜索（验证搜索有效性）
3. 重置功能：搜索后点击重置，表单清空（验证重置有效性）
4. 新增按钮可点击（验证入口可用性）
5. 标签页切换：我的订单/协同订单/全部订单切换（验证顶部标签页功能）
6. 搜索后标签页同步：搜索条件在我的订单/协同订单/全部订单间同步（验证搜索结果同步）
7. 底部详情标签页切换：销售子单/采购订单/样品流转（验证底部标签页功能）
8. 销售子单添加：点击销售子单标签的添加按钮（验证新增项目功能）
9. 采购订单添加：点击采购订单标签的添加按钮（验证新增项目功能）
10. 样品流转添加：点击样品流转标签的添加按钮（验证新增项目功能）
11. 销售合同关联：点击销售合同标签的关联按钮（验证关联功能）

## P1 场景（待补充）

1. 订单状态下拉筛选
2. 审批状态下拉筛选
3. 技术交付下拉筛选
4. 客户姓名下拉筛选
5. 客户机构下拉筛选
6. 报价单/合同/费用确认/对账/开票/付款操作
7. 导出功能
8. 实验信息/销售发票/销售资金等其他详情标签页

## 成功标准（断言口径）

- 页面加载完成 → URL 为 `/orders/order0`，表格可见，面包屑显示「销售主单管理」
- 搜索有效 → 输入关键词后表格数据更新
- 重置有效 → 点击重置后搜索表单清空，表格恢复全量数据
- 新增可用 → 新增按钮可见且可点击
- 标签页切换 → 切换后标签激活状态变化

## 已知坑

- **动态路由**：该页面路由由后端菜单动态生成，需确认测试账号有权限访问
- **登录态复用**：依赖 `storageState` 文件，首次运行需先执行登录用例或自动登录生成
- **表格数据**：表格数据来自后端接口，测试时需确保有测试数据
- **标签页切换**：切换标签页会触发新的数据请求，需等待加载完成
- **下拉框**：部分下拉框为远程搜索，输入后会触发 API 请求获取选项

## 登录态复用机制

同首页测试，通过 `storageState` 复用登录态：

```ts
const AUTH_FILE = 'tests/generated/.auth/user.json'

test.beforeAll(async ({ browser }) => {
  if (existsSync(AUTH_FILE)) return
})
test.use({ storageState: AUTH_FILE })
```
