# 页面：采购订单管理

- 路由：`/orders/order2`
- 前置：已登录（通过 storageState 复用登录态）
- 页面标题：米格实验室 - 采购订单管理
- 侧边栏路径：订单管理 → 采购订单管理

## 关键区域与精确定位器

| 区域 | 定位器 | 说明 |
|------|--------|------|
| 面包屑 | `getByRole('link', { name: '采购订单管理' })` | 当前页面面包屑 |
| 主单ID输入框 | `getByRole('textbox', { name: '主单ID' })` | 文本输入 |
| 订单状态下拉框 | `getByRole('combobox', { name: '订单状态' })` | 下拉选择 |
| 专家姓名下拉框 | `getByRole('combobox', { name: '专家姓名' })` | 下拉选择 |
| 专家机构输入框 | `getByRole('textbox', { name: '专家机构' })` | 文本输入 |
| 我方机构输入框 | `getByRole('textbox', { name: '我方机构' })` | 文本输入 |
| 搜索按钮 | `getByRole('button', { name: '搜索' })` | 搜索操作 |
| 重置按钮 | `getByRole('button', { name: '重置' })` | 重置表单 |
| 新增按钮 | `getByRole('button', { name: '新增' })` | 新增订单 |
| 对账按钮 | `getByRole('button', { name: '对账' })` | 对账操作 |
| 开票按钮 | `getByRole('button', { name: '开票' })` | 开票操作 |
| 付款按钮 | `getByRole('button', { name: '付款' })` | 付款操作 |
| 同步金蝶按钮 | `getByRole('button', { name: '同步金蝶' })` | 同步金蝶操作 |
| 金蝶红冲按钮 | `getByRole('button', { name: '金蝶红冲' })` | 金蝶红冲操作 |
| 技术催办按钮 | `getByRole('button', { name: '技术催办' })` | 技术催办操作 |
| 导出按钮 | `getByRole('button', { name: '导出' })` | 导出数据 |
| 全部数据下载按钮 | `getByRole('button', { name: '全部数据下载' })` | 下载全部数据 |
| 展开按钮 | `getByRole('button', { name: '展开' })` | 显示更多搜索条件 |
| 复制按钮 | `getByRole('button', { name: '复制' })` | 复制订单 |
| 编辑按钮 | `getByRole('button', { name: '编辑' })` | 编辑订单 |
| 更多按钮 | `getByRole('button', { name: '更多' })` | 更多操作 |
| 我的订单标签 | `getByRole('tab', { name: '我的订单' })` | 顶部标签页 |
| 机时预约标签 | `getByRole('tab', { name: '机时预约' })` | 底部详情标签页 |
| 样品流转标签 | `getByRole('tab', { name: '样品流转' })` | 底部详情标签页 |
| 采购合同标签 | `getByRole('tab', { name: '采购合同' })` | 底部详情标签页 |
| 采购资金标签 | `getByRole('tab', { name: '采购资金' })` | 底部详情标签页 |
| 添加按钮 | `getByRole('button', { name: '添加' })` | 底部详情区域添加按钮 |
| 关联按钮 | `getByRole('button', { name: '关联' })` | 关联功能按钮 |
| 数据表格 | `page.locator('.el-table').first()` | Element Plus 表格组件 |

## P0 场景

1. 页面加载完成，表格数据可见（验证页面可访问性）
2. 搜索功能：输入主单ID搜索（验证搜索有效性）
3. 重置功能：搜索后点击重置，表单清空（验证重置有效性）
4. 新增按钮可点击（验证入口可用性）
5. 标签页切换：我的订单/机时预约切换（验证标签页功能）
6. 订单状态筛选：使用订单状态下拉框筛选（验证筛选功能）
7. 专家姓名筛选：使用专家姓名下拉框筛选（验证筛选功能）
8. 专家机构搜索：输入专家机构搜索（验证搜索功能）
9. 我方机构搜索：输入我方机构搜索（验证搜索功能）
10. 新增采购订单：点击新增按钮弹出表单（验证新增入口）
11. 复制操作：点击表格行的复制按钮（验证复制功能）
12. 编辑操作：点击表格行的编辑按钮（验证编辑功能）
13. 更多操作：点击表格行的更多按钮（验证更多功能）
14. 样品流转添加：点击样品流转标签的添加按钮（验证新增项目功能）
15. 采购合同关联：点击采购合同标签的关联按钮（验证关联功能）
16. 采购资金关联：点击采购资金标签的关联按钮（验证关联功能）

## P1 场景（待补充）

1. 对账/开票/付款操作
2. 同步金蝶/金蝶红冲操作
3. 技术催办操作
4. 导出/全部数据下载
5. 展开搜索条件测试

## 成功标准（断言口径）

- 页面加载完成 → URL 为 `/orders/order2`，表格可见，面包屑显示「采购订单管理」
- 搜索有效 → 输入关键词后表格数据更新
- 重置有效 → 点击重置后搜索表单清空，表格恢复全量数据
- 新增可用 → 新增按钮可见且可点击
- 标签页切换 → 切换后标签激活状态变化

## 已知坑

- **动态路由**：该页面路由由后端菜单动态生成，需确认测试账号有权限访问
- **登录态复用**：依赖 `storageState` 文件，首次运行需先执行登录用例或自动登录生成
- **表格数据**：表格数据来自后端接口，测试时需确保有测试数据
- **标签页切换**：切换标签页会触发新的数据请求，需等待加载完成
- **下拉框**：部分下拉框为远程搜索，输入后会触发 API 请求获取选项

## 登录态复用机制

同首页测试，通过 `storageState` 复用登录态：

```ts
const AUTH_FILE = 'tests/generated/.auth/user.json'

test.beforeAll(async ({ browser }) => {
  if (existsSync(AUTH_FILE)) return
})
test.use({ storageState: AUTH_FILE })
```
